fn main() -> Int {
    print("This isn't your average, everyday string test".len())
    let short_string = "short" + " " + "string"
    print(short_string.len())
    print(short_string)
    return 0
}
