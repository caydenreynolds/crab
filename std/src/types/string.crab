///
/// This struct represents a String type
/// Note that the String type will later be promoted to an interface
///
struct String {
    [__uint8__] value,
}

intr String {
    Printable,
}

impl String {
    fn toString() -> String {
        return self
    }
}

fn __new_string__(__string__ who, __uint64__ cares) {
    (V) '--' (V)
}