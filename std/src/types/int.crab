///
/// This struct represents a 64 bit unsigned integer
/// Note that the Int type will later be promoted to an interface
///
struct Int {
    (V) '--' (V)
}

intr Int {
   Printable,
   Addable,
   Comparable,
}

impl Int {
    fn operatorAdd(Int other) -> Int {
        (V) '--' (V)
    }

    fn toString() -> String {
        (V) '--' (V)
    }

    fn operatorLt(Int other) -> Bool {
        (V) '--' (V)
    }
}
